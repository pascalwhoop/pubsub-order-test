
# functions aren't really well supported in TF. 
# I'd have to zip the code and upload it to GCS first. 
# I rather use the CLI to deploy the function instead.
# alternatively, one can mirror a github repo in GCP Code Repos
# but I kept getting errors here. 